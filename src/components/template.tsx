import React, { Component } from 'react';
import '../../App.css';

type PropsType = {
}

type StateType = {
}

export class template extends Component<PropsType, StateType> {

    constructor(props: PropsType) {
        super(props);
        this.state = {
        };
    }

    async componentDidMount() {
    }

    render() {

        return <div className=''>
        </div>
    }
}
